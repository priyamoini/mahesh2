
#include<stdio.h>
#include<conio.h>
void main()
{
    int i, n,  ;
    printf("Enter the limit : ") ;
    scanf("%d", &n) ;
    printf("\nEnter the number : ") 
    for(i = 1 ; i <= n ; i++)
    if(i % d == 0)
    printf("%d\t", i) ;
    getch();
    }
